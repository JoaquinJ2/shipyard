---
description: Configure this repo's shipyard overlay and materialize kernel + packs into .cursor/ (after Matt + livingdocs)
---

# /setup-shipyard

Follow the **`setup-shipyard`** skill exactly. Do not skip the gate.

After the overlay is written, materialize kernel and packs with `scripts/sync-cursor.sh` (no `--pull`). Do not copy `commands/`, `agents/`, or pack files by hand.
